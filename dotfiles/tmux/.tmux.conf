# Placeholder for tmux configuration. No custom bindings yet.

# Allow OSC passthrough so Claude Code's terminal notifications (OSC 9)
# reach the outer terminal (iTerm2) instead of being swallowed by tmux.
set -g allow-passthrough on

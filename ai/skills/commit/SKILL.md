---
name: commit 
description: Easily git add and commit changes, with a single command
compatibility: null
---

Add and commit the files that have been changed most recently in the current session:

```bash
git add <changed_files>` and `git commit -m "<1-sentence description>"
```

KEEP THE DESCRIPTION BRIEF. A single sentence will do.
